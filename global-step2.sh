#!/bin/zsh
cd reponame && git add filename && git checkout -b branchname && git commit -m "commit message" &&
git push origin HEAD && gh pr create --title "commit message" --fill &&
cd reponame && git add filename && git checkout -b branchname && git commit -m "commit message" &&
git push origin HEAD && gh pr create --title "commit message" --fill &&
cd reponame && git add filename && git checkout -b branchname && git commit -m "commit message" &&
git push origin HEAD && gh pr create --title "commit message" --fill &&
cd reponame && git add filename && git checkout -b branchname && git commit -m "commit message" &&
git push origin HEAD && gh pr create --title "commit message" --fill &&
cd reponame && git add filename && git checkout -b branchname && git commit -m "commit message" &&
git push origin HEAD && gh pr create --title "commit message" --fill &&